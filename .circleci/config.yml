version: 2.1

jobs:
  build&test:
    docker: 
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run: 
          command: npm i
          name: install dependencies
      - run:
          command: npm run build
          name: Build the app